<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
              android:background="@drawable/bg_activity_main"
    android:orientation="vertical"
              android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
              android:paddingTop="@dimen/activity_vertical_margin">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <ImageView android:id="@+id/iv_app_logo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
                   android:src="@drawable/hale_logo"
                   android:visibility="gone"/>

    </RelativeLayout>


    <LinearLayout  android:id="@+id/ll_container_buttons"
        android:layout_width="match_parent"
                   android:layout_height="@dimen/height_ll_container_buttons"
        android:gravity="center"
                   android:orientation="horizontal">

        <TextView
            android:id="@+id/btn_login"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/activity_horizontal_margin"
            android:background="@drawable/selector_btn_bg"
            android:paddingBottom="@dimen/padding_vertical_button"
            android:paddingLeft="@dimen/padding_horizontal_button"
            android:paddingRight="@dimen/padding_horizontal_button"
            android:paddingTop="@dimen/padding_vertical_button"
            android:text="@string/btn_login"
            android:textColor="@android:color/white"
            android:textSize="@dimen/text_size_button"/>

        <TextView
            android:id="@+id/btn_register"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/activity_horizontal_margin"
            android:background="@drawable/selector_btn_bg"
            android:paddingBottom="@dimen/padding_vertical_button"
            android:paddingLeft="@dimen/padding_horizontal_button"
            android:paddingRight="@dimen/padding_horizontal_button"
            android:paddingTop="@dimen/padding_vertical_button"
            android:text="@string/btn_register"
            android:textColor="@android:color/white"
            android:textSize="@dimen/text_size_button"/>

    </LinearLayout>

</LinearLayout>